# Backend API — uses the shared Kalypso CA for HTTPS.
#
# Prereqs:
#   1. Start the shared Kalypso:  cd ../  &&  docker compose up -d
#   2. Trust the CA once (see ../docker-compose.yml)
#   3. Add to /etc/hosts:  127.0.0.1 api.local
#
# Usage:
#   docker compose up -d
#   # Visit https://api.local:8443

services:
  # Sidecar fetches certs from the shared Kalypso
  certs:
    build: ../../..
    # image: kalypso/kalypso:latest
    command: ["sidecar"]
    environment:
      KALYPSO_DOMAINS: "api.local,*.api.local"
      KALYPSO_SERVER: "http://kalypso:8200"
    volumes:
      - certs:/certs
    networks:
      - kalypso

  api:
    image: nginx:alpine
    depends_on:
      certs:
        condition: service_started
    ports:
      - "8443:443"
    volumes:
      - certs:/etc/nginx/certs:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  certs:

networks:
  kalypso:
    external: true
